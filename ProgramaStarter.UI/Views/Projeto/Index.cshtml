@model IEnumerable<ProgramaStarter.Application.DTO.ProjetoDTO>

<h1>Projetos</h1>

<hr />
<div class="row">
    <div class="col-md-12">
        <div>
            <div class="pull-left">
                <a asp-action="Create" class="btn btn-primary">
                    <i title="Register New" class="fas fa-plus"></i> Novo Projeto
                </a>
            </div>
        </div>
    </div>
</div>
<br />

<table id="programas" class="table table-sm table-hover table-striped table-borderless">
    <thead>
        <tr class="table-light">
            <th>Id</th>
            <th>Modulo</th>
            <th>Etapa</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var projeto in Model)
        {
            <tr>
                <td class="align-middle">@projeto.Id</td>
                <td class="align-middle">@projeto.Modulo.Nome</td>
                <td class="align-middle">@projeto.Etapa</td>

                <td class="align-middle">
                    <button class="btn btn-secondary btn-sm">
                        <a asp-action="Edit" asp-route-id="@projeto.Id" title="Edit">
                            <i class="fa-regular fa-pen-to-square"></i> Editar</a>
                    </button>
                    <button class="btn btn-danger btn-sm"><a asp-action="Delete" asp-route-id="@projeto.Id" title="Delete">
                            <i class="fa-regular fa-trash-can"></i> Excluir</a>
                    </button>
                </td>
            </tr>
        }
    </tbody>

</table>